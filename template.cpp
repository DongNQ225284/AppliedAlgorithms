#include <bits/stdc++.h>
typedef long long ll;
#define FOR(i, a, b) for (ll i = (a); i <= (b); i++)

#define debug(x) cerr << #x << " = " << x << '\n';
#define debug_array(arr, a, b) cout << #arr << ": "; FOR(i, a, b) cerr << arr[i] << " "; cout << "\n";
#define stop do { char chr; std::cin >> chr; } while(0)
const ll INF = 1e10;
const ll MAX = 50;
using namespace std;


void input() {
    int a; cin >> a;
    debug(a);
}

int main() {
    input();
    
    
    return 0;
}